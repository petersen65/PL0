var x, y, z;

(* single line comment *)

begin { single line comment }
    x := 4; { single line comment }
    y :(* possible but not recommended *)= 5;
    
    z := -x {
        multi line comment
        } - y;

    ! x { single line comment };
    ! { single line comment } y;
    ! z;

    if odd (* 
        multi line comment
        x *) z then
            ! 42;
end.
